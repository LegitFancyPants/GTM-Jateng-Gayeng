node_modules
# Keep environment variables out of version control
.env

/generated/prisma

# SQLite database files (local only, use Neon for cloud DB)
dev.db
prisma/dev.db
*.db

# Local photo uploads (photos are stored in Cloudinary, not locally)
uploads/
temp/
